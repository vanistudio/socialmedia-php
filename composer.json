{
    "require": {
        "vlucas/phpdotenv": "^5.6",
        "pusher/pusher-php-server": "^7.2",
        "openai-php/client": "^0.18.0"
    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
